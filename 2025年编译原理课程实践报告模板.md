# 编译原理课程实践报告：闭门造车

元培学院 2200017713 张能源

## 一、编译器概述

### 1.1 基本功能

本编译器基本具备如下功能：
1. 将SysY代码转化到抽象语法树。
2. 通过抽象语法树，生成Koopa IR中间代码。
3. 将Koopa IR中间代码转化为RISC-V汇编指令。

### 1.2 主要特点

本编译器的主要特点是**基于栈的内存管理**、**保留的寄存器**、**良好可扩展性**。
1. 基于栈的内存管理是极少寄存器分配以及支持纯粹的递归调用。
2. 保留的寄存器是由于Koopa IR以三地址码为主，故我在这里保存t0,t1,t2专门用以运算使用，配合上一条使得在编写编译器时较为简单。
3. 良好可扩展性是指大量基于栈的简单封装函数使得在接洽扩展内容时易于调用，且优化可只对封装函数进行。

## 二、编译器设计

### 2.1 主要模块组成

编译器由4个主要模块组成：
1. **sysy.l**部分负责将SySy代码转为token序列。
2. **sysy.y**部分负责将token序列转化为AST。
3. **ast.hpp**部分负责将AST转为Koopa IR。
4. **koopatoriscv.cpp**将Koopa IR转为最终的RISC-V.

### 2.2 主要数据结构
#### Koopa IR生成

在ast.hpp中，我设计了一个Result，以便捷通过变量是否为立即数或否来对应输出内容。

```c
class Result{
    public:
     int type = IMM;
     int value;//存放对应寄存器或是值
     friend ostream& operator<<(ostream& os, const Result& result) {
         if(result.type == IMM) {
             os << result.value;
         } else {
             os << "%" << result.value;
         }
         return os;
     }
};
```

所有的AST都基于BaseAST，如下所示：

```c
class BaseAST {
    public:
    virtual ~BaseAST() = default;
   
    virtual Result Dump() const = 0;
};
```

在`if...else...`语句方面，由于涉及到二义性问题，所以本编译器在sysy.y处设计，对Stmt进行拆分，分为O(pen)Stmt以及M(atched)Stmt，并根据课件上分别对其定义

```c
Stmt:OStmt{}
   |MStmt{};
```

对于长度不定的token A，我将其拆为非左递归的Mul(ti)A以及A进行处理：

```c
MulInitVal
  : InitVal{
    auto ast = new MulInitValAST();
    ast->exp = unique_ptr<BaseAST>($1);
    ast -> keep = 0;
    $$ = ast;
  }|InitVal','MulInitVal{
    auto ast = new MulInitValAST();
    ast->exp = unique_ptr<BaseAST>($1);
    ast->kexp = unique_ptr<BaseAST>($3);
    ast -> keep = 1;
    $$ = ast;
  }
  ;
```

以上为共性，对短路求值等具体实现我在下文中对应章节提及。

#### RISC-V生成

在koopatoriscv.hpp里，我定义了Regmanager类，在里面封装了符号表信息，一些内置封装函数，以及输出对应RISC-V编码的示例函数

```c
class Regmanager {
  public:
    int regnum = 0;
    int stacknum = 0;
    int stack_size = 0;
    unordered_map<koopa_raw_value_t, RegOrStack> regmap;
    unordered_map<koopa_raw_value_t, RegOrStack> gregmap;

    void StoreToReg(const koopa_raw_value_t& value, Reg reg, const std::string& regname){
        //此函数为将栈上内容临时存入寄存器
    }

    void DumpBinary(const koopa_raw_binary_t& binary, const koopa_raw_value_t& value){
        //输出binary有关内容
    }
}
```

在koopatoriscv.cpp里，在进入函数块前，我为每一个koopa IR指令分配了栈空间(Koopa IR不会重复复制)

```c
for (size_t i = 0; i < func->bbs.len; ++i) {
      auto bb = reinterpret_cast<koopa_raw_basic_block_t>(func->bbs.buffer[i]);
      for (size_t j = 0; j < bb->insts.len; ++j) {
          auto inst = reinterpret_cast<koopa_raw_value_t>(bb->insts.buffer[j]);
          if (inst->ty->tag != KOOPA_RTT_UNIT) {
              ++cnt;
          }

          if(inst->kind.tag == KOOPA_RVT_CALL)
          {
            int numa =  inst->kind.data.call.args.len;
            call_cnt = max(call_cnt,numa-8);
            a=1;
          }
          if (inst->kind.tag == KOOPA_RVT_ALLOC) {
            int size = getsize(inst->ty->data.pointer.base);
            cnt += size/4;
          }
      }
    }
```



### 2.3 主要设计考虑及算法选择

#### 2.3.1 符号表的设计考虑

在生成Koopa IR时，我直接定义了装SymbolTable类的双端队列，在每次进入一个块之前，我都会将一个新的Table压入队列末端，每次定义都会在最末端块定义；当离开这个块时将其弹出；同时，我维护一个全局变量表，每当进入一个新的函数将全局变量表压入新的双端队列。

在寻找变量时，从队列末端向前搜索。对于相同变量a的别名，我将其命名为a_i，其中i为块的深度，全局变量i = 0。这样即使有错开的相同块定义了同一个变量，也会因为活跃时间不同而不产生冲突。

#### 2.3.2 寄存器分配策略
我并未对寄存器进行分配，仅保留t0,t1,t2作为临时变量，并在调用函数时将数据从栈中取出放入传参用寄存器，并将函数结果从a0压回栈里。

#### 2.3.3 采用的优化策略
无

#### 2.3.4 其它补充设计考虑
无

## 三、编译器实现

### 3.1 各阶段编码细节


#### Lv1. main函数和Lv2. 初试目标代码生成
```
加入正则表达式：LineComment   "//".*|\/\*(".*"|[^\*"]*|\*+[^\/])*\*\/

另外，我维护一个docker-compose.yml的文件，这样编写有及时性。

```
#### Lv3. 表达式
```
文档里的定义的语法规范提供了完整的运算顺序。

我的Dump函数返回一个上面的描述的Result数据作为返回值，这样立即数的运算可在编译阶段完成。这样对于Lv3的所有样例，可以直接返回一个值。

对于类似MulExp ::= UnaryExp | MulExp ("*" | "/" | "%") UnaryExp;语法规范，我将其拆为非左递归新文法。

```
#### Lv4. 常量和变量
```
在生成RISC-V汇编代码的过程中，我在调用主函数时会计算栈的长度。

在计算某一变量在栈上具体位置时，为避免出RISC-V的R型指令超过立即数域的情况，我将所有相关指令拆分成li和add两项输出。

```
#### Lv5. 语句块和作用域
```
在生成Koopa IR时，我直接定义了装SymbolTable类的双端队列，在每次进入一个块之前，我都会将一个新的Table压入队列末端，每次定义都会在最末端块定义；当离开这个块时将其弹出；同时，我维护一个全局变量表，每当进入一个新的函数将全局变量表压入新的双端队列。

在寻找变量时，从队列末端向前搜索。对于相同变量a的别名，我将其命名为a_i，其中i为块的深度，全局变量i = 0。这样即使有错开的相同块定义了同一个变量，也会因为活跃时间不同而不产生冲突。
```
#### Lv6. if语句
```
在`if...else...`语句方面，由于涉及到二义性问题，所以本编译器在sysy.y处设计，对Stmt进行拆分，分为O(pen)Stmt以及M(atched)Stmt，并根据课件上分别对其定义

Stmt:OStmt{}|MStmt{};

我最开始对短路求值的理解有误，仅对If里面的Exp进行了短路求值，后又更改之。

在这个短路求值中，对跳转位置的命名较为困难，我专门维护了一个表对其进行处理。

```
#### Lv7. while语句
```
while相较其他的函数比较好处理，遂简单完成之。
```
#### Lv8. 函数和全局变量
```
超过参数范围的将其放到栈上，并在进入这个函数时对参数最多call指令额外分配一些栈空间。

```
#### Lv9. 数组
```
在进入之前，我先将所有的内置函数都打印到Koopa IR中。

在koopatoriscv中定义了一个新的函数，此函数根据type递归求出每个数组的大小。

同时，我在ast.hpp中，将Result扩展，使其能够兼容数组指针作为Dump函数的返回值。

同时要区分getelementptr和getptr，前者是用于计算内存地址；而后者则是简单地返回一个已存储的指针（也即最早调用数组时）。
```

### 3.2 工具软件介绍（若未使用特殊软件或库，则本部分可略过）
1. `flex/bison`：使用`flex`进行词法分析，将源代码文件切割成一系列`token`。之后，利用`bison`定义的语法规则，对`flex`生成的`token`序列进行语法分析，并构建程序的抽象语法树 (AST)。
2. `libkoopa`：使用`libkoopa`库提供的C语言接口。通过遍历前端生成的抽象语法树 (AST)，将程序结构和语义信息转换为指定的中间表示 (Koopa IR)，为后续的目标代码生成做准备。
3. `make`：使用 `make`工具管理项目的编译流程。通过编写`Makefile`文件，定义源文件之间的依赖关系以及编译规则，实现对整个编译器项目的一键自动化编译、链接和清理，提高了开发效率。

### 3.3 测试情况说明（如果进行过额外的测试，可增加此部分内容）

我在本地跑的时候一般都是直接调lab所给予的本地样例进行比较，或精简本地代码的内容。

## 四、实习总结

### 4.1 收获和体会
本次编译原理课程实践让我收获颇丰。最大的感受是，我第一次完整地、亲手地将一门高级语言（SysY）翻译成了机器能够执行的底层汇编代码（RISC-V）。这个过程让我对计算机系统“从上到下”的整个执行流程有了前所未有的深刻理解。过去停留在理论层面的概念，如词法分析、语法分析、中间代码生成、目标代码生成等，都通过具体的编码实践变得鲜活和具体起来。

在设计上，我选择了基于栈的内存管理策略，并仅保留少数寄存器作临时使用 。这虽然不是性能最优的方案，但让我深刻体会到了编译器设计中的“权衡”（trade-off）：为了降低初期实现的复杂度，我牺牲了潜在的性能。这也让我明白了为什么现代编译器中寄存器分配算法如此重要和复杂。同时，通过对函数（如 StoreToReg）和类（如 Result, Regmanager）的封装 ，我学会了如何用工程化的思想去构建一个复杂的系统，这对于未来的软件开发工作无疑是大有裨益的。

### 4.2 学习过程中的难点，以及对实习过程和内容的建议
在实践过程中，我遇到了不少挑战。最大的难点集中在中间代码生成和目标代码生成阶段。

1. 逻辑的复杂性：处理 if-else 的悬挂问题和 && || 的短路求值逻辑是我花费时间最多的地方。特别是短路求值，我最初的理解有误，只对条件表达式本身进行了处理，后来才修正为正确的跳转逻辑。为这些跳转生成和管理唯一的标签名也相当棘手 。
2. 状态管理：无论是生成 Koopa IR 时用双端队列维护的符号表作用域 ，还是生成 RISC-V 时对栈帧布局（局部变量、函数参数、返回地址）的精确计算，都对状态的细致管理提出了很高要求。任何一个微小的计算失误，都可能导致程序在运行时发生难以调试的栈错误。
